- name: Configure Proxmox host
  hosts: proxmox
  become: true
  pre_tasks:
    - name: Wait for host to be reachable
      ansible.builtin.wait_for_connection:
        timeout: 300
  roles:
    - proxmox
