all:
  children:
    # Apollo — network stack
    proxmox:
      hosts:
        apollo:
          ansible_host: apollo.internal.thespragg.dev
          ansible_user: root
    apollo_lxcs:
      children:
        adblocking:
          hosts:
            adguard:
              ansible_host: 10.0.40.3
              ansible_user: root
        proxy:
          hosts:
            caddy:
              ansible_host: 10.0.40.4
              ansible_user: root
    # Titan — services
    bots:
      hosts:
        osrs-clan-bot:
          ansible_host: 10.10.0.21
          ansible_user: root
    databases:
      hosts:
        postgres:
          ansible_host: 10.10.0.22
          ansible_user: root
    monitoring:
      hosts:
        grafana:
          ansible_host: 10.10.0.23
          ansible_user: root
