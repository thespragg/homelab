all:
  children:
    # Apollo — network stack
    proxmox:
      hosts:
        apollo:
          ansible_host: 10.0.10.2
          ansible_user: root
    apollo_lxcs:
      children:
        adblocking:
          hosts:
            adguard:
              ansible_host: 10.0.40.3
              ansible_user: root
        proxy:
          hosts:
            caddy:
              ansible_host: 10.0.40.4
              ansible_user: root
        network_management:
          hosts:
            unifi:
              ansible_host: 10.0.40.5
              ansible_user: root
    # Titan — services
    bots:
      hosts:
        osrs-clan-bot:
          ansible_host: 10.0.40.6
          ansible_user: root
    databases:
      hosts:
        postgres:
          ansible_host: 10.0.40.7
          ansible_user: root
    monitoring:
      hosts:
        grafana:
          ansible_host: 10.0.40.8
          ansible_user: root
    media:
      hosts:
        titan:
          ansible_host: 10.0.40.200
          ansible_user: root
    documents:
      hosts:
        paperless:
          ansible_host: 10.0.40.9
          ansible_user: root
