- name: Install docker dependencies
  apt:
    name:
      - apt-transport-https
      - ca-certificates
      - curl
      - gnupg
    state: present
    update_cache: yes

- name: Create docker apt keyrings directory
  file:
    path: /etc/apt/keyrings
    state: directory
    mode: '0755'

- name: Download docker apt key
  get_url:
    url: https://download.docker.com/linux/{{ ansible_distribution | lower }}/gpg
    dest: /etc/apt/keyrings/docker.asc
    mode: '0644'

- name: Add docker apt repository
  deb822_repository:
    name: docker
    types: deb
    uris: https://download.docker.com/linux/{{ ansible_distribution | lower }}
    suites: "{{ ansible_distribution_release }}"
    components: stable
    signed_by: /etc/apt/keyrings/docker.asc
    state: present
    enabled: yes

- name: Install docker
  apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-compose-plugin
    state: present
    update_cache: yes

- name: Ensure docker is started and enabled
  systemd:
    name: docker
    state: started
    enabled: yes

- name: Create paperless directory
  file:
    path: "{{ paperless_dir }}"
    state: directory
    mode: '0755'

- name: Create paperless data directories
  file:
    path: "{{ item }}"
    state: directory
    mode: '0755'
  loop:
    - "{{ paperless_data_location }}"
    - "{{ paperless_media_location }}"
    - "{{ paperless_export_location }}"
    - "{{ paperless_consume_location }}"

- name: Place docker compose file
  template:
    src: docker-compose.yml.j2
    dest: "{{ paperless_dir }}/docker-compose.yml"
    mode: '0644'
  no_log: true

- name: Allow paperless through firewall
  ufw:
    rule: allow
    port: "{{ paperless_port }}"
    proto: tcp
    src: "{{ item }}"
  loop: "{{ paperless_allowed_hosts }}"

- name: Start paperless stack
  shell: "docker compose -f {{ paperless_dir }}/docker-compose.yml up -d --pull always"
  args:
    chdir: "{{ paperless_dir }}"
